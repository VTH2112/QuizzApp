
export const addUserInfo = (data) => {
    return {
        type: "addUserInfo",
        payload: data,
    }
}

export const addReward = (data) => {
    return {
        type: "addReward",
        payload: data,
    }
}